int best_index = -1;
int biggest_size = 0;

for (int i = 0; i < memory.size(); i++)
{
    if (memory[i].free && memory[i].size > biggest_size)
    {
        biggest_size = memory[i].size;
        best_index = i;
    }
}
